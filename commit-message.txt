feat(sandbox): add FakeWorkspace and scripted runner for graph testing

Create src/repro/sandbox/fake.py so Engineer A can simulate sandbox
outcomes without running real tests, matching Workspace's real
signatures exactly.

- FakeWorkspace and a scripted runner that returns a queue of prepared
  ExecutionResult objects
- Convenience constructors: green_result(), red_result(failed=1),
  timeout_result(), import_error_result()

Add a test using inspect.signature that asserts FakeWorkspace and
Workspace expose the same public method names and signatures. If the
two ever drift, this test breaks the build instead of letting
Engineer A's suite silently lie about the real system.

Unblocks Engineer A's graph test suite — post in team channel once merged.